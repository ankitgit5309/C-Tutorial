#include<stdio.h>

int main(int argc, char const *argv[])
{
    int i=5;
    printf("the value of i++ is %d\n",i++);//++i means pahle increment karo phir oprint karo and
    //  i++ pahle print karo phir increment karo.......
    // +++ operator exist nahi karta hai 
    // +=  compound assignment operators.
        printf("The value of i is %d\n",i);
    return 0;
}